# Production Environment Variables for Vercel
# Copy this to .env.production.local and fill in your values

# Backend API URL (your Railway backend URL)
# Leave empty to use relative URLs (handled by Vercel rewrites)
VITE_API_URL=
